---
import Posts from '../../layouts/Posts.astro';
import { getPublishedPosts } from '../../utils/content';

const posts = await getPublishedPosts();
---

<Posts posts={posts} pageTitle="Posts" pageDesc="All the articles I've posted." />
